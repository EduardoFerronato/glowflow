# Banco de dados — SQLite em dev. Para produção, troque para MySQL
# (ver README.md, seção "Migrando para MySQL").
DATABASE_URL="file:./dev.db"

# Better Auth — gere um novo com `openssl rand -hex 32`
BETTER_AUTH_SECRET=""
BETTER_AUTH_URL="http://localhost:3000"
NEXT_PUBLIC_BETTER_AUTH_URL="http://localhost:3000"

# UploadThing — https://uploadthing.com (opcional em dev)
UPLOADTHING_TOKEN=""
